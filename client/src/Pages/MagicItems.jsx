// this will be a page where users can view the list of possible available magic items in the shop

import magicItems from '../db/magicItems.json';

export default function MagicItems(){
    return (
        
    );
}